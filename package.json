{
  "name": "vite-svelte-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "git subtree push --prefix dist origin gh-pages",
    "force-publish": "git push origin `git subtree split --prefix dist master`:gh-pages --force"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.9",
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.18",
    "sass": "^1.55.0",
    "svelte": "^3.52.0",
    "svelte-preprocess": "^4.10.7",
    "vite": "^3.1.8"
  }
}
